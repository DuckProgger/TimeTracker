using UI.Views;

namespace UI.Infrastructure.Constants;
internal static class ViewNames
{
    public const string WorksViewName = nameof(WorkdayView);
    public const string ScreenshotsViewName = nameof(ScreenshotsView);
}
